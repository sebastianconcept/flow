A CQAPICommand class is an abstract class that has in its children the concrete commands of this API.

Subclasses perform the concrete reactions to the commands so they are the ones defining behavior 